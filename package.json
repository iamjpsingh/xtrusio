{
  "name": "xtrusio",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "api-types:generate": "set -a; . ./.env; set +a; pnpm --filter @xtrusio/api-types run generate"
  },
  "devDependencies": {
    "turbo": "^2.9.16",
    "typescript": "^5.5.0",
    "prettier": "^3.3.0"
  }
}
